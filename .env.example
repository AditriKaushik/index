DATABASE_URL="postgresql://app:app_dev_password@localhost:5432/social_platform?schema=public"

# --- Auth secrets (dev only — replace in production) ---
JWT_ACCESS_SECRET="dev-access-secret-change-me"
JWT_REFRESH_SECRET="dev-refresh-secret-change-me"

# --- WebAuthn (passkeys / biometric relogin) ---
WEBAUTHN_RP_ID="localhost"
WEBAUTHN_RP_NAME="Social Platform"
WEBAUTHN_ORIGIN="http://localhost:3000"

# --- OTP delivery providers (mocked by default; wire real providers here) ---
OTP_PROVIDER="console"
